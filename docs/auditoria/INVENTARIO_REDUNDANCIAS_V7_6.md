# Inventário de redundâncias V7.6

## Consolidadas
- `raid-back-top`, `back-to-top` e `podium-scroll-btn` → `.site-back-top`.
- `league-notice-*` e `raid-floating-*` → `action-feedback-*`.
- plataforma legada do Registro → `.page-hero-mascots::after`.
- `comparison-alert` e `renderComparisonAlert()` removidos do Hall público.

## Mantidas por responsabilidade diferente
- `raid-status-card`: status inline do formulário.
- `raid-state-card`: estado persistente da área de resultados.
